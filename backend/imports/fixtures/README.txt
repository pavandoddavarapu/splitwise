# Place expenses_export.csv here before running Step 7.
# This directory is committed to git so the path is stable.
# The CSV file itself is NOT committed (to avoid accidental edits).
# Add to .gitignore if needed, or commit it explicitly as a read-only artifact.
